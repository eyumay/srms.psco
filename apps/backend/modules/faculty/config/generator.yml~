generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Faculty
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          faculty
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  
        display: [ faculty_name, description ]
      list:    
        display: [ faculty_name, description ]
      filter:  
        display: [ faculty_name, description ]
      form:    ~
      edit:    ~
      new:     ~
